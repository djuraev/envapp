apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  APP_ENV: "production"
  APP_VERSION: "1.0"
  APP_COLOR: "blue"
  DB_HOST: "postgres.default.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "mydb"
  FEATURE_FLAGS: "dark-mode,new-dashboard"
